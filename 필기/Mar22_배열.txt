이전에 실행되었던 프로그램의 0, 1 비트 데이터를 가비지 value 라고 함.
배열을 선언하면 그 안의 가비지를 모두 비트 수준에서 0으로 초기화 해줌.

b = new int[3]; 로 선언하면, 참조가 됨

b.length == 3; 로 변수가 만들어짐

int c[];
//c = new int[5];
c = b;

대입 과정: 데이터 타입이 동일한가? -> 아니라면 형일치가 가능한가? -> 아니라면 에러 발생