class Answers{
    public static void main(String[] args){
        /*

        //0. 화면에 Hello JAVA라는 문자열을 출력하라
        System.out.println("Hello JAVA");

        //0. 정수형 변수를 선언하고 적당한 값을 대입한 후 출력하는 프로그램을 작성하라
        int n = 5;
        System.out.println(n);

        //0. 커맨드라인 전달인자를 사용하여 정수 하나를 입력 받아 그 수를 출력하는 프로그램을 작성하라
        System.out.println(args[0]);

        //1. 자신의 이름을 출력하라
        System.out.println("홍길동");

        //2. 정수형 변수 n의 값이 짝수인 경우에만 자신의 이름을 출력하라.
        int n = 10;
        if (n%2 == 0) System.out.println("홍길동");

        //3. 정수형 변수 m의 값이 짝수인 경우에만 자신과 친구의 이름을 출력하라
        int m = 10;
        if (m%2 == 0){
            System.out.println("홍길동");
            System.out.println("김철수");
        }

        //4. 정수형 변수 a가 짝수인 경우 자신의 이름을, 홀수인 경우 친구의 이름을 출력하라
        int a = 10;
        if (a%2 == 0) System.out.println("내이름");
        else System.out.println("친구이름");
        
        //5. 정수형 변수 c와 d중에서 더 큰 값을 출력하라
        int c = 10;
        int d = 11;
        if (c > d) System.out.println(c);
        else System.out.println(d);

        //6. 정수형 변수 p와 q중에서 더 큰 값은 big에 작은 값은 small에 대입한 후 big과 small을 출력하시오.
        int p = 10;
        int q = 11;
        if (p > q) {
            big = p;
            small = q;
        }
        else {
            big = q;
            small = p;
        }
        System.out.println(big);
        System.out.println(small);

        //7. 삼각형의 세변의 길이를 의미하는 s, t, u로 삼각형이 이루어지는지 아닌지 여부를 출력하라
        int s = 10;
        int t = 11;
        int u = 12;
        if (s + t > u && t + u > s && s + u > t) System.out.println("삼각형이 존재");
        else System.out.println("삼각형이 존재하지 않음");

        //8. 정수형 변수 x와 y와 z중에서 가장 큰 값을 출력하라
        int x = 10;
        int y = 11;
        int z = 12;
        if (x > y){
            if (x > z) System.out.println(x);
            else System.out.println(z);
        }
        else {
            if (y > z) System.out.println(y);
            else System.out.println(z);
        }

        //9. 정수형 변수 x와 y와 z중에서 가장 큰 값을 출력하라
        
        */

    }
}